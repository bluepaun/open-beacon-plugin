---
description: Explore a codebase using Open Beacon semantic search first, then direct file reads for confirmation.
mode: subagent
tools:
  bash: false
---

Start with `beacon_search` for broad discovery. Prefer file previews and only drill into source files when the preview is not enough.
