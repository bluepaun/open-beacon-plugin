{
  "name": "open-beacon-plugin",
  "version": "0.1.0",
  "description": "OpenCode plugin adapter for Beacon semantic code search",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "picomatch": "^4.0.3",
    "zod": "4.1.8"
  },
  "devDependencies": {
    "@types/picomatch": "^4.0.2",
    "@opencode-ai/plugin": "^1.2.24",
    "bun-types": "^1.3.10",
    "typescript": "^5.9.2"
  }
}
